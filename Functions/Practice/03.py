# Write a function full_name(first, last) that takes first name and last name as parameters and returns a single string in the format "First Last".

def Full_name(First, Last):
    print(First,Last)

(Full_name("Rhydam", "Kumar"))