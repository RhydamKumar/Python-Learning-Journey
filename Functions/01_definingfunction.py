def average(a, b): # Defined a function named average that takes two parameters a and b

    c = (a + b) / 2
    return (c)

average(10, 20)
average(5, 15) # Calling the function with different arguments


a1 = average(10, 20) # Calling the function and storing the result in a variable a1
a2 = average(5, 15) # Calling the function and storing the result in a variable a2

print (a1 + a2) # This will print None because the average function does not return any value, it only prints the result.   